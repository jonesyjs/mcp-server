server:
  name: "Agent-Specs"
  port: 8223

plugins:
  personas:
    enabled: true
    skillsPath: ~/Desktop/Project/skills/skills

  gtasks:
    enabled: true
    credentialsPath: ~/path/to/gcp-oauth.keys.json
    tokenPath: ~/path/to/.gtasks-token.json

