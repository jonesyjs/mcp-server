server:
  name: "Agent-Specs"
  port: 8223

plugins:
  personas:
    enabled: true
    skillsPath: /skills

  gtasks:
    enabled: false
    credentialsPath: /secrets/gcp-oauth.keys.json
    tokenPath: /secrets/.gtasks-token.json

